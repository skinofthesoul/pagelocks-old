{% extends 'partials/base.html.twig' %}

{% if admin.route %}
    {% set context = admin.page(true) %}
{% endif %}

{% block titlebar %}
    <h1><i class="fa fa-fw fa-file-text-o"></i> {{ "PLUGIN_PAGELOCKS.LOCKS"|tu }}</h1>
{% endblock %}

{% block content %}
    <h1>{{ "PLUGIN_PAGELOCKS.LOCKSBYUSERS"|tu }}</h1>

    <div class="admin-block">
        <table>
            <thead>
                <th class="author">Author</th>
                <th class="route">Page</th>
                <th class="last-seen">Last active on page</th>
                <th class="delete"></th>
            </thead>
            <tbody id="locklist">
                {# {% for email, lock in pagelocks.locks %}
                    <tr>
                        <td class="author">{{email}}</td>
                        <td class="route">{{lock.route}}</td>
                        <td class="last-seen">{{ lock.timestamp }}</td>
                    </tr>
                {% endfor %} #}
            </tbody>
        </table>

    </div>
{% endblock %}


